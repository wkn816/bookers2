 <link rel="stylesheet" href="books.scss" />

<div class = "container">
 <div class = "row">

<%= render 'books/list', book: @book, user: @user %>

  <div class = "col-md-8 offset-md-1">
<div class ="aa">
<h1>Books</h1>
</div>
<table class="table">
<thead>
  <tr>
     <th></th><th>title</th> <th>Opinion</th>
  </tr>
</thead>

<% @books.each do |book| %>
<tbody>
<tr>
  <td><span><%= link_to attachment_image_tag(book.user, :profile_image, :fill, 40, 40, format: 'jpeg', fallback: "no_image.jpg"), user_path(book.user_id) %></span></td>

  <td><span> <%= link_to book.title, book_path(book.id) %> </span></td>
  <td><span> <%= book.body %></span></td>
</tr>
</tbody>
<% end %>

</table>
  </div>
 </div>
</div>