<h1>Editing Book</h1>
<!-- form_with部分 -->
<%= form_with model:@book, local: true do |book| %>
  <h4>title</h4>
  <%= book.text_field :title %>
  <h4>Opinion</h4>
  <%= book.text_field :body %>
  
  <%= book.submit 'UpdeteBook' %>
<% end %>